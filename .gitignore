# Python
__pycache__/
*.py[cod]
*.pyo
.venv/

# ---- Real-TCGA run ---------------------------------------------------------
# results/tcga/ IS committed — it is the study's output. Only the raw download
# and the 680 MB VAE checkpoint are excluded.
*.zip
*.pt

# ---- Toy-data pipeline intermediates ---------------------------------------
# All rebuildable — see scripts/README.md for the command that regenerates each.
data/toyforVAE_811_mRNA@.tsv
data/toyforVAE_mRNA@_binary.csv
data/prepared/
data/embeddings/

# Trained checkpoints and logs (too large for git)
results/vae/
results/saved_model/
results/test_ci_dae.tsv
results/logs/
results/00000000/

# Jupyter / IDE
.ipynb_checkpoints/
.DS_Store
