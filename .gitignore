# Python
__pycache__/
*.py[cod]
*.pyo
.venv/

# ---- Real-TCGA run (the actual study) --------------------------------------
# Unpack vaecox_results.zip from the Kaggle run here; results are reproducible
# from the notebook, so they are not committed.
out/
*.zip

# ---- Toy-data pipeline intermediates ---------------------------------------
# All rebuildable — see scripts/README.md for the command that regenerates each.
data/toyforVAE_811_mRNA@.tsv
data/toyforVAE_mRNA@_binary.csv
data/prepared/
data/embeddings/

# Trained checkpoints and logs (too large for git)
results/vae/
results/saved_model/
results/test_ci_dae.tsv
results/logs/
results/00000000/

# Jupyter / IDE
.ipynb_checkpoints/
.DS_Store
